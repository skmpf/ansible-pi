- name: Setup Git global configuration
  git_config:
    scope: global
  with_items:
    - { name: "core.editor", value: "{{ editor }}" }
    - { name: "user.email", value: "{{ git_user_email }}" }
    - { name: "user.name", value: "{{ git_user_name }}" }
